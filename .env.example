# Copy to .env for local host-run Next.js (npm run dev on the host).
# When the web service runs INSIDE docker compose, compose sets DATABASE_URL to
# use the "db" service host instead (see docker-compose.yml).
DATABASE_URL=postgresql://calc:calc@localhost:5432/margin_calculator
